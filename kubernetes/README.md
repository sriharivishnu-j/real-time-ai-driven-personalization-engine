# Kubernetes Deployment for AI Personalization Engine

This directory contains Kubernetes configuration files for deploying the AI-driven personalization engine on AWS EKS.

## Deployment

Apply the deployment with:

```bash
kubectl apply -f deployment.yaml
```
